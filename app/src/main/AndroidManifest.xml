<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright (c).
  ~ TT-Technologies IT-Service GmbH
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.scan.barcode">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.CAMERA"/>

    <uses-feature android:name="android.hardware.camera"/>
    <uses-feature android:name="android.hardware.camera.autofocus"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".presentation.ScanApplication"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".presentation.main.MainAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.BarcodeScannerAct"/>
        <activity android:name=".presentation.login.LoginAct" />
        <activity android:name=".presentation.barcode.BarcodeAct" />
        <activity android:name=".presentation.qr2nd.Qr2ndAct" />
        <activity android:name=".presentation.qr1st.Qr1stAct" />
    </application>

</manifest>